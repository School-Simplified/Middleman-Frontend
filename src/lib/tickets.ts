import axios from "axios";
const TIMMY_API_URL = "https://api.timmy.schoolsimplfied.org";
export const createBreakRequestTicket = async (start: Date, end: Date) => {
  // given the start and end date, creates a break request ticket using the Timmy API.
};
